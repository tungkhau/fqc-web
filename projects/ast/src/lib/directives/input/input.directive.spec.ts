import { InputDirective } from './input.directive';

describe('AstInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDirective();
    expect(directive).toBeTruthy();
  });
});
