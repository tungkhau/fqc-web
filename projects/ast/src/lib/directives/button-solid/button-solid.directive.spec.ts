import { ButtonSolidDirective } from './button-solid.directive';

describe('ButtonSolidDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSolidDirective();
    expect(directive).toBeTruthy();
  });
});
